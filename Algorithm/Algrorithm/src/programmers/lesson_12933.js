//  정수 내림차순 배치(정렬)
function solution(n) {
    let answer = 0;
    const arr = [];
    arr.push(n);

    console.log(arr);
    
    // arr.sort(function (a, b) {    //  내림차순
    //     return b - a;
    // })
    // answer = arr;
    return answer;
}